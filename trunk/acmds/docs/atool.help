
usage: atool [OPTION]... [ARGUMENT]...

  atool was designed to act like a filter for command-line
  tasks with the basic sub-commands:

  split - a Filter to split the stdin into libmsgque objects
  cut   - a Filter to cut libmsgque objects into columns
  sort  - a Filter to sort columns
  join  - a Filter to join columns into a string and send it to stdout

  please use "sub-command -h" or "sub-command --help" for help

