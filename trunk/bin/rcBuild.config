#	mount -o bind /proc /chroot/proc
#	mount -o bind /dev /chroot/dev
#	mount -o bind /dev/pts /chroot/dev/pts
#	mount -o bind /sys /chroot/sys
#	mount -o bind /sys/fs/cgroup /chroot/sys/fs/cgroup
#
#	cp /etc/HOSTNAME /chroot/etc/
#	cp /etc/resolv.conf /chroot/etc/
#	
#	#add user - save id as in original 
#	#example: nhi1:x:1001:100:NHI1:/home/nhi1:/bin/bash
#	useradd -m -u 1001 -g 100 -k /etc/skel -s /bin/bash -d /home/nhi1 -c NHI1 nhi1
#	groupadd -g 1000 nhi1
#
# /etc/ssh/sshd_config
#	PermitRootLogin no
#	PasswordAuthentication no
#	AllowGroups nhi1
#	ChrootDirectory /chroot
#	#LogLevel Debug3
#	X11Forwarding no
#	
#	chmod o+w /var/run/uscreens/
#	
#	ssh aotto1968@shell.berlios.de
#	ssh nhi1@nhi1.no-ip.org
#	
#	Frage: gruppe trusted?
#	
#	# .ssh/authorized_keys
#	masgeblich ist /home/nhi1/.ssh/authorized_keys
#	und !! NICHT !! /chroot/home/nhi1/.ssh/authorized_keys

# chroot directory to use as the 'root' directory
rcBuild_chroot=/chroot

# build directory to use for temporary build files
# used for: mount -t tmpfs -o size=$rcBuild_size tmpfs $rcBuild_directory
rcBuild_directory=/build
rcBuild_size=1g


