set -x
export PATH=$HOME/ext/$MACHTYPE/thread/bin:$PATH
$(dirname $(readlink $0))/../configure    --prefix=$HOME/ext/$MACHTYPE/thread \
                --enable-symbols \
                --enable-threads \
		--enable-cxx \
		--enable-tcl \
		--enable-java \
                $@

#               --enable-static

