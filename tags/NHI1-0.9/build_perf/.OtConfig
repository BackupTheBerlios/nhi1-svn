set -x
export PATH=$HOME/ext/$MACHTYPE/thread/bin:$PATH
../configure    --prefix=$HOME/ext/$MACHTYPE/thread \
                --enable-threads \
                --enable-java \
                $@

#               --enable-static

